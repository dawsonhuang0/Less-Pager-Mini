export interface Config {
  row: number;
  col: number;

  window: number;
  halfWindow: number;

  screenWidth: number;
  halfScreenWidth: number;

  chopLongLines: boolean;

  indentation: number;
}
